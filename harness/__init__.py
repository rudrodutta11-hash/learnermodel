"""Conversation validation harness — runs Kai against the real model and
scores the output against our behavior rules. Measurement only; it imports
and reuses the shipping prompts verbatim and never modifies them."""
